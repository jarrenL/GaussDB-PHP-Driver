# 已知限制

当前实现已经解决 M/O 跨平台基础连接、UTF-8、布尔和二进制差异，但以下边界仍然存在：

| 范围 | 当前结论 | 建议 |
|---|---|---|
| PHP 版本 | 代码最低 PHP 7.2.34；实测 PHP 7.2.34 和 PHP 8.3 | PHP 7.2 已停止官方安全维护；其他 PHP 小版本上线前跑完整契约 |
| 驱动版本 | 必须使用与服务端版本、部署形态、操作系统和 CPU 架构匹配的官方 ODBC | 任何客户端或服务端变化都必须复验 |
| TIMESTAMP 微秒 | M/O ODBC 路径可能只返回秒 | 不承诺微秒；需要时拆成整数微秒字段 |
| `lastInsertId()` | PDO_ODBC 返回 `IM001`；本地 M AUTO_INCREMENT 语义也不稳定 | 使用业务键，或在受控 SQL 中显式返回生成值 |
| M TEXT | 长度上限受目标实例和字段定义影响 | 大对象选型需按客户实例专项验证，不在文档中固化版本相关阈值 |
| 二进制列 | ODBC 以十六进制文本返回；元数据不足以安全自动识别所有列 | 查询时显式标注 `ResultType::BINARY_HEX` |
| ORA RAW 入参 | 需要十六进制绑定，与 M 原始 LOB 绑定不同 | 必须使用 `BinaryValue`，不要绕过兼容层 |
| ODBC UDT | 官方 ODBC 不支持用户自定义类型及其存储过程参数 | 改用基础类型/JSON，或单独评估其他官方驱动 |
| 存储过程 OUT 参数 | 开启特定兼容选项时，官方 ODBC 存在限制 | 在目标库对每个过程做专项验收 |
| SSL | 当前测试环境没有 TLS 成功基线 | 生产要求 TLS 时，在启用证书的实例验证 require/证书校验 |
| 连接池状态 | 会话 GUC、临时表可能污染复用连接 | 归还连接前执行官方建议的状态清理 |

网络故障注入、自动重连、死锁、长时间压力、Laravel/Doctrine ORM、RPM/DEB 包和性能基准尚未纳入当前交付。兼容层不会吞掉 PDOException，也不会自动重试非幂等 SQL。
